(feature
    (section_keyword) @context
    (title) @name) @item

(section
    (section_keyword) @context
    (title)? @name) @item

(step
    (step_keywords) @context
    (step_definition) @name
    ) @item

(docstring
    (docstring_start
        (docstring_lang) @name
    )
    ) @item

(tag) @name @item

(table
    (table_header) @name
    ) @item
